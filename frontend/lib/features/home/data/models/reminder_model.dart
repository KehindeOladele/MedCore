import 'package:flutter/material.dart';

class ReminderModel {
  // Placeholder for now as the UI uses a widget mostly
  // But strictly, we might want:
  final String title;

  const ReminderModel({this.title = "Reminder"});
}
