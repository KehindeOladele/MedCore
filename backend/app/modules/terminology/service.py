def search_codes(system: str, query: str):
    # Temporary stub (replace with DB or external API later)
    return [
        {
            "system": system,
            "code": "123456",
            "display": f"{query.title()} Example"
        }
    ]
